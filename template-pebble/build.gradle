description = 'Ligero template engine adapter backed by Pebble'

dependencies {
    api project(':core')
    implementation libs.pebble

    testImplementation libs.junit.jupiter
    testRuntimeOnly libs.junit.launcher
    testImplementation libs.assertj.core
}
