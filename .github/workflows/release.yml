name: Publish to Maven Central

# Runs when a GitHub Release is published. The version is taken from the tag
# (e.g. tag `v0.3.0` -> version `0.3.0`), so there's nothing to bump by hand.
on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'          # the framework compiles with Java 21
          distribution: 'temurin'
          cache: 'gradle'

      - name: Publish + release to Maven Central
        # publishToSonatype uploads the signed artifacts to a staging repo;
        # closeAndReleaseSonatypeStagingRepository closes and releases it.
        run: >
          ./gradlew publishToSonatype closeAndReleaseSonatypeStagingRepository
          -Pversion=${GITHUB_REF_NAME#v}
          --no-daemon --stacktrace
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_PASSWORD }}
          ORG_GRADLE_PROJECT_signingKey: ${{ secrets.GPG_PRIVATE_KEY }}
          ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.GPG_PASSPHRASE }}
